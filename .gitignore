node_modules/
*.vsix
dist/
out/
.DS_Store
.plan-runner/

# Vendored standalone Plan Runner — reference only, never shipped/committed
# (root-anchored so it does NOT match planning/reference/)
/reference/
